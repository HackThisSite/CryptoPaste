language: php

php:
  - '7.0'
  - '7.1'

before_script:
  - composer self-update
  - composer install

script:
  - phpunit tests/

notifications:
  irc:
    channels:
      - "irc.hackthissite.org#cryptopaste"
    use_notice: true
    skip_join: true
