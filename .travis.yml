language: php

php:
  - '7.0'
  - '7.1'

before_script:
  - echo "extension = json.so" >> ~/.phpenv/versions/$(phpenv version-name)/etc/php.ini
  - composer self-update
  - composer install

notifications:
  irc:
    channels:
      - "irc.hackthissite.org#cryptopaste"
    use_notice: true
    skip_join: true
